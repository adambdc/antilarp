# Worklist — optional fuel for the autonomous loop

A simple checkbox list of candidate limits to probe. Markers:
`[ ]` untried · `[x]` done, no limit · `[>]` crossed · `[~]` parked · `[!]` substrate-hard

- [ ] no script to normalize timestamps across the input files  (wl-0001)
- [ ] the report build is run by hand each time  (wl-0002)
- [ ] unsure whether the upstream API paginates past 100 results  (wl-0003)
- [ ] the nightly job has no failure alert  (wl-0004)
- [ ] can the working set fit in memory at 10x the current size?  (wl-0005)
